# ===========================
#   DATABASE - SUPABASE
# ===========================
spring.datasource.url=jdbc:postgresql://db.hgnucpfqgwqxbjijrefj.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=UniConnect2024*
spring.datasource.driver-class-name=org.postgresql.Driver

# ===========================
#     JPA / HIBERNATE
# ===========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Fix LOB (necesario para PostgreSQL + Hibernate)
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Evita LazyInitializationException
spring.jpa.open-in-view=false

# Supabase NO usa scripts SQL automáticos
spring.sql.init.mode=never
